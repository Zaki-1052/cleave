# 2026-03-27 — Phase 4.4: Peak Calling Sub-tabs (Gap Closure)

## What Was Done

- Phase 4.4 was already implemented in Phases 4.2/4.3 (all frontend components + backend endpoints). This session closed the remaining gaps:
- **FRiP score file generation**: Added `_write_frip_csv()` helper and `frip_scores.csv` output with `file_category: "frip"` in both `run()` and `mock_run()`. The "FRiP Score" category in the Files sub-tab dropdown now returns actual files.
- **Log file registration in real mode**: Added glob-based log file registration inside the per-reaction loop in `run()`, matching mock mode behavior.
- **Fixed 2 broken test assertions**: Phase 4.3 changed `top_peaks` from `"qc_report"` to its own category but didn't update `test_mock_run_output_categories` and `test_mock_run_reaction_ids_assigned`. Both now expect `{"qc_report", "top_peaks", "frip"}`.
- **3 new tests**: `test_write_frip_csv`, `test_mock_run_frip_csv_produced`, `test_mock_run_frip_file_registered`.

## Files Modified

- `backend/pipelines/peak_calling.py` — Added `_FRIP_CSV_HEADERS`, `_write_frip_csv()`, FRiP CSV registration in `run()` + `mock_run()`, log file registration in `run()`
- `backend/tests/test_peak_calling_pipeline.py` — Fixed 2 assertions, added 3 new tests, added `_write_frip_csv` import

## Test Results

52 tests passing in test_peak_calling_pipeline.py (49 existing + 3 new), 0 regressions. ruff check + format clean. tsc --noEmit clean.

## Phase 4 Done Criteria Status (All Complete)

- [x] All 5 peak caller modes work (MACS2 narrow/broad, SICER2, SEACR stringent/relaxed)
- [x] Fragment filter (<120bp) applied by default before calling
- [x] IgG control correctly assigned per reaction
- [x] SEACR preprocessing chain (MACS2 bdg → integer conversion → SEACR) works
- [x] FRiP calculation produces scores >0.2 for good enrichment targets
- [x] HOMER annotates peaks to genomic features
- [x] QC report shows FRiP table and annotation stacked bar chart
- [x] Peak calling files browsable by category (BED, FRiP, Annotation)
