# 2026-03-27 — Phase 4.3: Peak Calling QC Report — Annotation Chart + Bug Fixes + Tests

## What Was Done

- **Bug fix**: `top_peaks` file resolution — pipeline now registers `top_called_peaks.csv` with `file_category: "top_peaks"` (was `"qc_report"`, same as metrics CSV, causing it to never be found). Added `_resolve_output_by_name()` fallback for backward compatibility with old jobs.
- **Peak Annotation stacked bar chart** (Recharts) — first chart in the codebase:
  - Backend: HOMER `annotation_stats.txt` parser with prefix-based CUTANA Cloud category mapping (10 categories: Promoter, Exon, Intron, Intergenic, 3UTR, 5UTR, TTS, ncRNA, miRNA, pseudo)
  - Backend: `PeakAnnotationResult` Pydantic schema, `annotations` field on `PeakCallingQCReport`
  - Backend: `/jobs/{job_id}/peak-qc-report/annotation-csv` download endpoint
  - Frontend: `PeakAnnotationChart.tsx` — Recharts `<BarChart layout="vertical">` with 10 stacked color-coded bars
  - Frontend: SVG-to-canvas PNG export (zero-dependency), CSV download button
  - Integrated into `PeakCallingQCReportPanel.tsx` with "About Peak Annotation Plots" info sidebar section
- **Mock pipeline**: Varied annotation data per IgG vs target (was identical for all reactions)
- **8 new backend tests** for peak calling QC endpoints (14 total in test_qc_report.py)

## Files Modified

- `backend/schemas/qc_report.py` — Added `PeakAnnotationResult`, updated `PeakCallingQCReport`
- `backend/services/qc_report_service.py` — Added `_resolve_output_by_name`, `_resolve_all_outputs`, `_classify_annotation`, `_parse_annotation_stats`, `get_peak_annotation_csv`; fixed top_peaks resolution
- `backend/routers/jobs.py` — Added `/jobs/{job_id}/peak-qc-report/annotation-csv` endpoint
- `backend/pipelines/peak_calling.py` — Fixed top_peaks `file_category`; varied mock annotation data by IgG vs target
- `frontend/src/api/types.ts` — Added `PeakAnnotationResult`, updated `PeakCallingQCReport`
- `frontend/src/api/jobs.ts` — Added `downloadPeakAnnotationCsv()`
- `frontend/src/components/peak-calling/PeakAnnotationChart.tsx` — **NEW** Recharts stacked bar chart
- `frontend/src/components/peak-calling/PeakCallingQCReportPanel.tsx` — Integrated chart + info sidebar
- `backend/tests/test_qc_report.py` — 8 new peak calling QC tests

## Test Results

14 tests passing in test_qc_report.py (6 alignment + 8 peak calling), 0 regressions. TypeScript type check clean. ruff check + format clean.
