# 2026-03-24 — Phase 1.6-1.7: Notifications + Settings (Frontend Wiring)

## What was done

### Phase 1.6: Notifications (3 new files, 2 edits)
- Created `frontend/src/api/notifications.ts` — `getNotifications()`, `markRead()` API functions
- Created `frontend/src/hooks/useNotifications.ts` — `useNotifications()` (30s polling), `useUnreadCount()` (derived), `useMarkNotificationRead()` mutation with cache invalidation
- Created `frontend/src/components/layout/NotificationPanel.tsx` — dropdown panel with per-type SVG icons (briefcase for invitations, checkmark for jobs, bell for welcome), unread highlight, click-to-navigate + mark-read, click-outside-to-close
- Added `formatDateTime()` to `frontend/src/lib/utils.ts`
- Rewrote `frontend/src/components/layout/Navbar.tsx` — bell icon with red unread count badge, notification dropdown, user dropdown menu (Settings link + Sign Out), mutual exclusion between dropdowns

### Phase 1.7: Settings (1 new file, 4 edits)
- Added `updateMe()` to `frontend/src/api/auth.ts`
- Extracted `AuthContextValue` interface + `AuthContext` object into `frontend/src/contexts/authContextValue.ts` (fixes react-refresh lint warning)
- Updated `frontend/src/contexts/AuthContext.tsx` to export only `AuthProvider`, exposed `refreshUser` (maps to existing `fetchUser` callback)
- Updated `frontend/src/hooks/useAuth.ts` to import from `authContextValue.ts`
- Added `EMAIL_NOTIFICATION_OPTIONS` to `frontend/src/lib/constants.ts`
- Rewrote `frontend/src/pages/SettingsPage.tsx` — read-only email, editable first/last name, email notification dropdown, change detection, save calls `updateMe()` then `refreshUser()` to update Navbar

### Bonus fix
- Removed unused `Input` import from `frontend/src/components/experiments/CreateExperimentModal.tsx`

## Decisions made
- 30s polling for notifications (SSE deferred to Phase 3 per spec)
- Settings uses direct async/await + `refreshUser()` rather than TanStack Query mutation — consistent with how all auth operations work in this codebase
- Split `AuthContext.tsx` into component-only file + `authContextValue.ts` for non-component exports — standard fix for react-refresh warning
- `updateMe` placed in `auth.ts` alongside `getMe` rather than a separate `users.ts` — keeps user operations cohesive

## Open items
- Phase 1.8: Test stubs for notifications and settings endpoints
- Backend tests not runnable locally (need Docker for fastapi_users) — 0 backend files changed so no regression risk

## Key file paths
- `frontend/src/api/notifications.ts` — new
- `frontend/src/hooks/useNotifications.ts` — new
- `frontend/src/components/layout/NotificationPanel.tsx` — new
- `frontend/src/contexts/authContextValue.ts` — new (extracted from AuthContext)
- `frontend/src/components/layout/Navbar.tsx` — rewritten
- `frontend/src/pages/SettingsPage.tsx` — rewritten
